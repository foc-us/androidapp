package us.foc.transcranial.dcs.bluetooth;

/**
 * Bluetooth connection status (between the Android device and the TDC device).
 */
public enum ConnectionStatus {
    DISCONNECTED,
    CONNECTING,
    CONNECTED,
    PLAYING
}
